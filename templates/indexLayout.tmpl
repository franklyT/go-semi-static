<!doctype html>
{{define "indexLayout"}}
    <html>
        <head>
            <meta charset="utf-8">
            <title>{{template "title"}}</title>
            <link href="/static/styles/style.css" rel="stylesheet">
        </head>
        <body>
            {{template "body"}}
            <footer><p>Made with Go</p></footer>
        </body>
    </html>
{{end}}
