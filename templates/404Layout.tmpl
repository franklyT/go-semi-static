<!doctype html>
{{define "404Layout"}}
    <html>
        <head>
            <meta charset="utf-8">
            <title>{{template "title"}}</title>
            <link href="/static/styles/style.css" rel="stylesheet">
        </head>
        <body>
            {{template "body"}}
        </body>
    </html>
{{end}}
